version : "3"

services:
 api :
  build : ./api
  ports : 
   - "5000:5000"
  container_name : zero-trust-api

 worker :
  build : ./worker
  container_name : zero-trust-worker

 frontend :
  build : ./frontend
  ports:
    - "3000:80"
  container_name: zero-trust-frontend
  